# Base URL of the deployed backend (Express server), no trailing slash.
# In local dev this defaults to http://localhost:4000 if unset.
VITE_API_URL=http://localhost:4000

# Base URL of the separate Flask chatbot service, if used.
VITE_CHATBOT_API_URL=http://localhost:5000
